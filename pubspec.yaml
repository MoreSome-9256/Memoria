name: photo_album
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  photo_manager: ^3.8.3
  objectbox: ^5.3.1
  objectbox_flutter_libs: ^5.3.1
  path_provider: ^2.1.5
  dio: ^5.9.0
  google_mlkit_image_labeling: ^0.14.2
  google_mlkit_face_detection: ^0.13.2
  google_mlkit_text_recognition: ^0.15.1
  # ➕ 新增：底层原生图片压缩库
  flutter_image_compress: ^2.4.0
  # ➕ 动态权限申请库（定位必备）
  permission_handler: ^12.0.1
  flutter_markdown_plus: ^1.0.7
  onnxruntime: ^1.4.1
  image: ^4.2.0
  ffi: ^2.1.3
  shared_preferences: ^2.5.3
  flutter_local_notifications: ^21.0.0
  audioplayers: ^6.6.0
  flutter_animate: ^4.5.2
  ffmpeg_kit_flutter_new: ^4.1.0
  gal: ^2.3.0
  file_picker: ^11.0.2
  path: ^1.9.0
  crypto: ^3.0.6
  amplify_auth_cognito: 2.6.1
  amplify_flutter: 2.6.1
  cached_network_image: ^3.4.1
  flutter_riverpod: ^3.3.1
  go_router: ^17.1.0
  image_picker: ^1.2.1
  intl: ^0.20.2
  riverpod_annotation: ^4.0.2
  uuid: ^4.5.3
  flutter_quick_video_encoder: ^1.7.2
  share_plus: ^10.0.0
#  runanywhere: ^0.16.0
#  runanywhere_onnx: ^0.16.0      # STT, TTS, VAD
#  runanywhere_llamacpp: ^0.16.0  # LLM text generation

#dependency_overrides:
#  sqflite_android: 2.4.1

dev_dependencies:
  amplify_lints: ^3.0.0
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  objectbox_generator: ^5.3.1
  flutter_launcher_icons: ^0.14.4
#  riverpod_lint: ^2.1.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/logo.png
    - assets/images/welcome_bg.jpg
    - assets/expanded_tags_taxonomy.json
    - assets/ncnn/README.md
    - assets/ncnn/mobileclip_s2/image_encoder.ncnn.param
    - assets/ncnn/mobileclip_s2/image_encoder.ncnn.bin
    - assets/ncnn/mobileclip_s2/text_encoder.ncnn.param
    - assets/ncnn/mobileclip_s2/text_encoder.ncnn.bin
    - assets/ncnn/mobileclip_s2/projection_layer.ncnn.param
    - assets/ncnn/mobileclip_s2/projection_layer.ncnn.bin
    - assets/mobileclip2/s2/vision_model.onnx
    - assets/mobileclip2/s2/text_model.onnx
    - assets/face/   
    - ai_tools/test_student.png
    - ai_tools/test1.png
    - ai_tools/test_youleyuan.png
    - assets/clip_tokenizer/
    # - mobileclip_vision_ir9.onnx
    - assets/audio/sandal_leap.mp3
    - assets/audio/sandal_leap_beats.json
    - assets/audio/Hachimi.mp3
    - assets/audio/Hachimi_beats.json
    - assets/images/noise.png
    - assets/images/frame_horizontal.png
    - assets/images/frame_vertical.png
    - assets/transitions/orange_and_red.mov
    - assets/transitions/orange_and_red.webp
    - assets/transitions/white_circle.mov
    - assets/transitions/white_circle.webp
    - assets/transitions/flowers.mov
    - assets/transitions/flowers.webp
    - assets/transitions/light.mov
    - assets/transitions/light.webp
    - assets/transitions/blue_circle.mov
    - assets/transitions/blue_circle.webp
    - assets/transitions/red.mov
    - assets/transitions/red.webp
    - assets/transitions/green.mov
    - assets/transitions/green.webp
    - assets/transitions/glass.mov
    - assets/transitions/glass.webp
    - assets/transitions/mosaic.mov
    - assets/transitions/mosaic.webp
    - assets/audio/premade/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
